## Objetivo\n\nDeixar a tela `/acesso` (acesso público por perfil) confortável no celular e garantir que o build com PWA não falhe.\n\n## O que está errado hoje\n\n1. **Mobile**\n   - `pagina_acesso.tsx`: padding `py-10` e título `text-3xl` apertam o conteúdo em telas pequenas; rodapé com slugs em `font-mono` estoura largura no mobile.\n   - `card_perfil_acesso.tsx`: padding `p-6`, ícone fixo `h-12 w-12` e gap `gap-5` deixam o card grande demais no celular; sem `min-w-0` o texto pode causar overflow horizontal.\n   - Faltam `safe-area-inset-top/bottom` para celulares com notch e modo standalone (PWA instalado).\n\n2. **Erro de PWA no build**\n   - Já foi corrigido em mensagem anterior: `vite.config.ts` agora define `workbox.maximumFileSizeToCacheInBytes: 5 MB`, eliminando o erro\n     `Configure \